/**
 * Gate API
 * Welcome to Gate API APIv4 provides operations related to spot, margin, and contract trading, including public interfaces for querying market data and authenticated private interfaces for implementing API-based automated trading.
 *
 * Contact: support@mail.gate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/* tslint:disable:no-unused-locals */
import { InlineResponse2008 } from '../model/inlineResponse2008';
import { InlineResponse2009 } from '../model/inlineResponse2009';
import { InlineResponse400 } from '../model/inlineResponse400';
import { ObjectSerializer } from '../model/models';
import { ApiClient } from './apiClient';
import { AxiosRequestConfig, AxiosResponse } from 'axios';

// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================

export class SquareApi {
    protected client: ApiClient;

    constructor(client?: ApiClient) {
        if (client) {
            this.client = client;
        } else {
            this.client = new ApiClient();
        }
    }

    /**
     * Dynamic search endpoint for AI MCP platform. All parameters are passed via query string. Returns simplified fields. Designed for LLM function calling / MCP tool scenarios.
     * @summary AI MCP Dynamic Search
     * @param opts Optional parameters
     * @param opts.keyword Search keyword (currency name or content keyword, e.g., BTC, ETH)
     * @param opts.currency Filter by currency (exact currency code, e.g., BTC, ETH, SOL)
     * @param opts.timeRange Time range: 0 &#x3D; unlimited (default), 1 &#x3D; last day, 2 &#x3D; last week, 3 &#x3D; last month
     * @param opts.sort Sort order: 0 &#x3D; most popular (default), 1 &#x3D; latest
     * @param opts.limit Return count, 1-50, default 10
     * @param opts.page Page number
     */
    public async listSquareAiSearch(opts?: {
        keyword?: string;
        currency?: string;
        timeRange?: 0 | 1 | 2 | 3;
        sort?: 0 | 1;
        limit?: number;
        page?: number;
    }): Promise<{ response: AxiosResponse; body: InlineResponse2008 }> {
        const localVarPath = this.client.basePath + '/social/message/search';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.keyword !== undefined) {
            let keywordSerialized = ObjectSerializer.serialize(opts.keyword, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(keywordSerialized)) {
                keywordSerialized = keywordSerialized.join(',');
            }
            localVarQueryParameters['keyword'] = keywordSerialized;
        }

        if (opts.currency !== undefined) {
            let currencySerialized = ObjectSerializer.serialize(opts.currency, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(currencySerialized)) {
                currencySerialized = currencySerialized.join(',');
            }
            localVarQueryParameters['currency'] = currencySerialized;
        }

        if (opts.timeRange !== undefined) {
            let timeRangeSerialized = ObjectSerializer.serialize(opts.timeRange, '0 | 1 | 2 | 3');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(timeRangeSerialized)) {
                timeRangeSerialized = timeRangeSerialized.join(',');
            }
            localVarQueryParameters['time_range'] = timeRangeSerialized;
        }

        if (opts.sort !== undefined) {
            let sortSerialized = ObjectSerializer.serialize(opts.sort, '0 | 1');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(sortSerialized)) {
                sortSerialized = sortSerialized.join(',');
            }
            localVarQueryParameters['sort'] = sortSerialized;
        }

        if (opts.limit !== undefined) {
            let limitSerialized = ObjectSerializer.serialize(opts.limit, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitSerialized)) {
                limitSerialized = limitSerialized.join(',');
            }
            localVarQueryParameters['limit'] = limitSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = [];
        return this.client.request<InlineResponse2008>(config, 'InlineResponse2008', authSettings);
    }

    /**
     * AI assistant live stream/replay search endpoint. Filters live rooms or replay videos by business tags and currency. Each record in the returned list is distinguished by content_type: streaming = live broadcast (live field has value), video = replay video (video field has value). The number of results is controlled by the limit parameter (max 10), no additional pagination needed.
     * @summary Gate AI Assistant live stream data retrieval
     * @param opts Optional parameters
     * @param opts.tag Business type filter. Available values: Market Analysis, Hot Topics, Blockchain, Others
     * @param opts.coin Filter by currency name (e.g., BTC, ETH)
     * @param opts.sort Sort order: hot &#x3D; most popular (default), new &#x3D; latest
     * @param opts.limit Return count, 1-10, default 3
     */
    public async listLiveReplay(opts?: {
        tag?: string;
        coin?: string;
        sort?: 'hot' | 'new';
        limit?: number;
    }): Promise<{ response: AxiosResponse; body: InlineResponse2009 }> {
        const localVarPath = this.client.basePath + '/social/live/tag_coin_live_replay';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.tag !== undefined) {
            let tagSerialized = ObjectSerializer.serialize(opts.tag, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(tagSerialized)) {
                tagSerialized = tagSerialized.join(',');
            }
            localVarQueryParameters['tag'] = tagSerialized;
        }

        if (opts.coin !== undefined) {
            let coinSerialized = ObjectSerializer.serialize(opts.coin, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(coinSerialized)) {
                coinSerialized = coinSerialized.join(',');
            }
            localVarQueryParameters['coin'] = coinSerialized;
        }

        if (opts.sort !== undefined) {
            let sortSerialized = ObjectSerializer.serialize(opts.sort, "'hot' | 'new'");
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(sortSerialized)) {
                sortSerialized = sortSerialized.join(',');
            }
            localVarQueryParameters['sort'] = sortSerialized;
        }

        if (opts.limit !== undefined) {
            let limitSerialized = ObjectSerializer.serialize(opts.limit, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitSerialized)) {
                limitSerialized = limitSerialized.join(',');
            }
            localVarQueryParameters['limit'] = limitSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = [];
        return this.client.request<InlineResponse2009>(config, 'InlineResponse2009', authSettings);
    }
}
