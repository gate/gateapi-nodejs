/**
 * Gate API
 * Welcome to Gate API APIv4 provides operations related to spot, margin, and contract trading, including public interfaces for querying market data and authenticated private interfaces for implementing API-based automated trading.
 *
 * Contact: support@mail.gate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/* tslint:disable:no-unused-locals */
import { CreateOrderV4 } from '../model/createOrderV4';
import { InlineResponse20010 } from '../model/inlineResponse20010';
import { LaunchPoolV4CreateOrderResponse } from '../model/launchPoolV4CreateOrderResponse';
import { LaunchPoolV4ErrorResponse } from '../model/launchPoolV4ErrorResponse';
import { LaunchPoolV4PledgeRecord } from '../model/launchPoolV4PledgeRecord';
import { LaunchPoolV4Project } from '../model/launchPoolV4Project';
import { LaunchPoolV4RewardRecord } from '../model/launchPoolV4RewardRecord';
import { RedeemV4 } from '../model/redeemV4';
import { ObjectSerializer } from '../model/models';
import { ApiClient } from './apiClient';
import { AxiosRequestConfig, AxiosResponse } from 'axios';

// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================

export class LaunchApi {
    protected client: ApiClient;

    constructor(client?: ApiClient) {
        if (client) {
            this.client = client;
        } else {
            this.client = new ApiClient();
        }
    }

    /**
     * Retrieve the list of available LaunchPool projects, including basic project information and reward pool configuration. This endpoint does not require user authentication.
     * @summary Query LaunchPool project list
     * @param opts Optional parameters
     * @param opts.status Filter by project status: 0 for all, 1 for ongoing, 2 for warming up, 3 for ended, 4 for ongoing and warming up
     * @param opts.mortgageCoin Exact match by staking currency
     * @param opts.searchCoin Fuzzy match by reward currency and name
     * @param opts.limitRule Limit rule: 0 for regular pool, 1 for beginner pool
     * @param opts.sortType Sort type: 1 for max APR descending, 2 for max APR ascending
     * @param opts.page Page number, default 1
     * @param opts.pageSize Number of items per page, default 10, maximum 30
     */
    public async listLaunchPoolProjects(opts?: {
        status?: number;
        mortgageCoin?: string;
        searchCoin?: string;
        limitRule?: number;
        sortType?: number;
        page?: number;
        pageSize?: number;
    }): Promise<{ response: AxiosResponse; body: Array<LaunchPoolV4Project> }> {
        const localVarPath = this.client.basePath + '/launch/project-list';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.status !== undefined) {
            let statusSerialized = ObjectSerializer.serialize(opts.status, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(statusSerialized)) {
                statusSerialized = statusSerialized.join(',');
            }
            localVarQueryParameters['status'] = statusSerialized;
        }

        if (opts.mortgageCoin !== undefined) {
            let mortgageCoinSerialized = ObjectSerializer.serialize(opts.mortgageCoin, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(mortgageCoinSerialized)) {
                mortgageCoinSerialized = mortgageCoinSerialized.join(',');
            }
            localVarQueryParameters['mortgageCoin'] = mortgageCoinSerialized;
        }

        if (opts.searchCoin !== undefined) {
            let searchCoinSerialized = ObjectSerializer.serialize(opts.searchCoin, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(searchCoinSerialized)) {
                searchCoinSerialized = searchCoinSerialized.join(',');
            }
            localVarQueryParameters['searchCoin'] = searchCoinSerialized;
        }

        if (opts.limitRule !== undefined) {
            let limitRuleSerialized = ObjectSerializer.serialize(opts.limitRule, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitRuleSerialized)) {
                limitRuleSerialized = limitRuleSerialized.join(',');
            }
            localVarQueryParameters['limitRule'] = limitRuleSerialized;
        }

        if (opts.sortType !== undefined) {
            let sortTypeSerialized = ObjectSerializer.serialize(opts.sortType, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(sortTypeSerialized)) {
                sortTypeSerialized = sortTypeSerialized.join(',');
            }
            localVarQueryParameters['sortType'] = sortTypeSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        if (opts.pageSize !== undefined) {
            let pageSizeSerialized = ObjectSerializer.serialize(opts.pageSize, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSizeSerialized)) {
                pageSizeSerialized = pageSizeSerialized.join(',');
            }
            localVarQueryParameters['pageSize'] = pageSizeSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = [];
        return this.client.request<Array<LaunchPoolV4Project>>(config, 'Array<LaunchPoolV4Project>', authSettings);
    }

    /**
     * Create a new staking order for asset staking mining. This endpoint requires API Key signature authentication.
     * @summary Create LaunchPool staking order
     * @param createOrderV4
     */
    public async createLaunchPoolOrder(
        createOrderV4: CreateOrderV4,
    ): Promise<{ response: AxiosResponse; body: LaunchPoolV4CreateOrderResponse }> {
        const localVarPath = this.client.basePath + '/launch/create-order';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        // verify required parameter 'createOrderV4' is not null or undefined
        if (createOrderV4 === null || createOrderV4 === undefined) {
            throw new Error(
                'Required parameter createOrderV4 was null or undefined when calling createLaunchPoolOrder.',
            );
        }

        const config: AxiosRequestConfig = {
            method: 'POST',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
            data: ObjectSerializer.serialize(createOrderV4, 'CreateOrderV4'),
        };

        const authSettings = ['apiv4'];
        return this.client.request<LaunchPoolV4CreateOrderResponse>(
            config,
            'LaunchPoolV4CreateOrderResponse',
            authSettings,
        );
    }

    /**
     * Redeem staked assets and end staking mining. This endpoint requires API Key signature authentication.
     * @summary Redeem LaunchPool staked assets
     * @param redeemV4
     */
    public async redeemLaunchPool(redeemV4: RedeemV4): Promise<{ response: AxiosResponse; body: InlineResponse20010 }> {
        const localVarPath = this.client.basePath + '/launch/redeem';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        // verify required parameter 'redeemV4' is not null or undefined
        if (redeemV4 === null || redeemV4 === undefined) {
            throw new Error('Required parameter redeemV4 was null or undefined when calling redeemLaunchPool.');
        }

        const config: AxiosRequestConfig = {
            method: 'POST',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
            data: ObjectSerializer.serialize(redeemV4, 'RedeemV4'),
        };

        const authSettings = ['apiv4'];
        return this.client.request<InlineResponse20010>(config, 'InlineResponse20010', authSettings);
    }

    /**
     * Query user\'s staking and redemption operation records. This endpoint requires user authentication.
     * @summary Query user pledge records
     * @param opts Optional parameters
     * @param opts.page Page number, default 1
     * @param opts.pageSize Number of items per page, maximum 30
     * @param opts.type Type: 1 for pledge, 2 for redemption
     * @param opts.startTime Start time, format: YYYY-MM-DD HH:MM:SS
     * @param opts.endTime End time, format: YYYY-MM-DD HH:MM:SS
     * @param opts.coin Collateral currency
     */
    public async listLaunchPoolPledgeRecords(opts?: {
        page?: number;
        pageSize?: number;
        type?: number;
        startTime?: string;
        endTime?: string;
        coin?: string;
    }): Promise<{ response: AxiosResponse; body: Array<LaunchPoolV4PledgeRecord> }> {
        const localVarPath = this.client.basePath + '/launch/user-pledge-records';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        if (opts.pageSize !== undefined) {
            let pageSizeSerialized = ObjectSerializer.serialize(opts.pageSize, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSizeSerialized)) {
                pageSizeSerialized = pageSizeSerialized.join(',');
            }
            localVarQueryParameters['pageSize'] = pageSizeSerialized;
        }

        if (opts.type !== undefined) {
            let typeSerialized = ObjectSerializer.serialize(opts.type, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(typeSerialized)) {
                typeSerialized = typeSerialized.join(',');
            }
            localVarQueryParameters['type'] = typeSerialized;
        }

        if (opts.startTime !== undefined) {
            let startTimeSerialized = ObjectSerializer.serialize(opts.startTime, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(startTimeSerialized)) {
                startTimeSerialized = startTimeSerialized.join(',');
            }
            localVarQueryParameters['startTime'] = startTimeSerialized;
        }

        if (opts.endTime !== undefined) {
            let endTimeSerialized = ObjectSerializer.serialize(opts.endTime, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(endTimeSerialized)) {
                endTimeSerialized = endTimeSerialized.join(',');
            }
            localVarQueryParameters['endTime'] = endTimeSerialized;
        }

        if (opts.coin !== undefined) {
            let coinSerialized = ObjectSerializer.serialize(opts.coin, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(coinSerialized)) {
                coinSerialized = coinSerialized.join(',');
            }
            localVarQueryParameters['coin'] = coinSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = ['apiv4'];
        return this.client.request<Array<LaunchPoolV4PledgeRecord>>(
            config,
            'Array<LaunchPoolV4PledgeRecord>',
            authSettings,
        );
    }

    /**
     * Query the user\'s staking reward records. This endpoint requires user authentication.
     * @summary Query user reward records
     * @param opts Optional parameters
     * @param opts.page Page number, default 1
     * @param opts.pageSize Number of items per page, maximum 30
     * @param opts.startTime Start timestamp
     * @param opts.endTime End Timestamp
     * @param opts.coin Reward currency
     */
    public async listLaunchPoolRewardRecords(opts?: {
        page?: number;
        pageSize?: number;
        startTime?: number;
        endTime?: number;
        coin?: string;
    }): Promise<{ response: AxiosResponse; body: Array<LaunchPoolV4RewardRecord> }> {
        const localVarPath = this.client.basePath + '/launch/get-user-reward-records';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        if (opts.pageSize !== undefined) {
            let pageSizeSerialized = ObjectSerializer.serialize(opts.pageSize, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSizeSerialized)) {
                pageSizeSerialized = pageSizeSerialized.join(',');
            }
            localVarQueryParameters['pageSize'] = pageSizeSerialized;
        }

        if (opts.startTime !== undefined) {
            let startTimeSerialized = ObjectSerializer.serialize(opts.startTime, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(startTimeSerialized)) {
                startTimeSerialized = startTimeSerialized.join(',');
            }
            localVarQueryParameters['start_time'] = startTimeSerialized;
        }

        if (opts.endTime !== undefined) {
            let endTimeSerialized = ObjectSerializer.serialize(opts.endTime, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(endTimeSerialized)) {
                endTimeSerialized = endTimeSerialized.join(',');
            }
            localVarQueryParameters['end_time'] = endTimeSerialized;
        }

        if (opts.coin !== undefined) {
            let coinSerialized = ObjectSerializer.serialize(opts.coin, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(coinSerialized)) {
                coinSerialized = coinSerialized.join(',');
            }
            localVarQueryParameters['coin'] = coinSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = ['apiv4'];
        return this.client.request<Array<LaunchPoolV4RewardRecord>>(
            config,
            'Array<LaunchPoolV4RewardRecord>',
            authSettings,
        );
    }
}
