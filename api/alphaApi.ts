/**
 * Gate API
 * Welcome to Gate API APIv4 provides operations related to spot, margin, and contract trading, including public interfaces for querying market data and authenticated private interfaces for implementing API-based automated trading.
 *
 * Contact: support@mail.gate.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

/* tslint:disable:no-unused-locals */
import { AccountBookResponse } from '../model/accountBookResponse';
import { AccountsResponse } from '../model/accountsResponse';
import { AlphaTicker } from '../model/alphaTicker';
import { Currency2 } from '../model/currency2';
import { OrderResponse } from '../model/orderResponse';
import { PlaceOrderRequest } from '../model/placeOrderRequest';
import { PlaceOrderResponse } from '../model/placeOrderResponse';
import { QuoteRequest } from '../model/quoteRequest';
import { QuoteResponse } from '../model/quoteResponse';
import { Tokens } from '../model/tokens';
import { ObjectSerializer } from '../model/models';
import { ApiClient } from './apiClient';
import { AxiosRequestConfig, AxiosResponse } from 'axios';

// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================

export class AlphaApi {
    protected client: ApiClient;

    constructor(client?: ApiClient) {
        if (client) {
            this.client = client;
        } else {
            this.client = new ApiClient();
        }
    }

    /**
     * Query position assets
     * @summary Alpha Account API
     */
    public async listAlphaAccounts(): Promise<{ response: AxiosResponse; body: Array<AccountsResponse> }> {
        const localVarPath = this.client.basePath + '/alpha/accounts';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = ['apiv4'];
        return this.client.request<Array<AccountsResponse>>(config, 'Array<AccountsResponse>', authSettings);
    }

    /**
     * Query asset transactions
     * @summary Alpha Account Transaction History API
     * @param from Start timestamp for the query
     * @param opts Optional parameters
     * @param opts.to End timestamp for the query, defaults to current time if not specified
     * @param opts.page Page number
     * @param opts.limit Maximum 100 items per page
     */
    public async listAlphaAccountBook(
        from: number,
        opts: { to?: number; page?: number; limit?: number },
    ): Promise<{ response: AxiosResponse; body: Array<AccountBookResponse> }> {
        const localVarPath = this.client.basePath + '/alpha/account_book';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        // verify required parameter 'from' is not null or undefined
        if (from === null || from === undefined) {
            throw new Error('Required parameter from was null or undefined when calling listAlphaAccountBook.');
        }

        opts = opts || {};
        let fromSerialized = ObjectSerializer.serialize(from, 'number');
        // For array query parameters with style:form and explode:false, convert to comma-separated string
        if (Array.isArray(fromSerialized)) {
            fromSerialized = fromSerialized.join(',');
        }
        localVarQueryParameters['from'] = fromSerialized;

        if (opts.to !== undefined) {
            let toSerialized = ObjectSerializer.serialize(opts.to, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(toSerialized)) {
                toSerialized = toSerialized.join(',');
            }
            localVarQueryParameters['to'] = toSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        if (opts.limit !== undefined) {
            let limitSerialized = ObjectSerializer.serialize(opts.limit, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitSerialized)) {
                limitSerialized = limitSerialized.join(',');
            }
            localVarQueryParameters['limit'] = limitSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = ['apiv4'];
        return this.client.request<Array<AccountBookResponse>>(config, 'Array<AccountBookResponse>', authSettings);
    }

    /**
     * The quote_id returned by the price inquiry interface is valid for one minute; an order must be placed within this minute. If it times out, a new price inquiry is required. Rate-limited at 10 requests per second per user.
     * @summary Alpha Quote API
     * @param quoteRequest
     */
    public async quoteAlphaOrder(
        quoteRequest: QuoteRequest,
    ): Promise<{ response: AxiosResponse; body: QuoteResponse }> {
        const localVarPath = this.client.basePath + '/alpha/quote';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        // verify required parameter 'quoteRequest' is not null or undefined
        if (quoteRequest === null || quoteRequest === undefined) {
            throw new Error('Required parameter quoteRequest was null or undefined when calling quoteAlphaOrder.');
        }

        const config: AxiosRequestConfig = {
            method: 'POST',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
            data: ObjectSerializer.serialize(quoteRequest, 'QuoteRequest'),
        };

        const authSettings = ['apiv4'];
        return this.client.request<QuoteResponse>(config, 'QuoteResponse', authSettings);
    }

    /**
     *
     * @summary Alpha Order List API
     * @param opts Optional parameters
     * @param opts.currency Trading symbol
     * @param opts.side Buy or sell orders - buy - sell
     * @param opts.status Order Status - &#x60;0&#x60; : All - &#x60;1&#x60; : Processing - &#x60;2&#x60; : Successful - &#x60;3&#x60; : Failed - &#x60;4&#x60; : Cancelled - &#x60;5&#x60; : Buy order placed but transfer not completed - &#x60;6&#x60; : Order cancelled but transfer not completed
     * @param opts.from Start time for order query
     * @param opts.to End time for order query, defaults to current time if not specified
     * @param opts.limit Maximum number of items returned. Default: 100, minimum: 1, maximum: 100
     * @param opts.page Page number
     */
    public async listAlphaOrder(opts: {
        currency?: string;
        side?: string;
        status?: number;
        from?: number;
        to?: number;
        limit?: number;
        page?: number;
    }): Promise<{ response: AxiosResponse; body: Array<OrderResponse> }> {
        const localVarPath = this.client.basePath + '/alpha/orders';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.currency !== undefined) {
            let currencySerialized = ObjectSerializer.serialize(opts.currency, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(currencySerialized)) {
                currencySerialized = currencySerialized.join(',');
            }
            localVarQueryParameters['currency'] = currencySerialized;
        }

        if (opts.side !== undefined) {
            let sideSerialized = ObjectSerializer.serialize(opts.side, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(sideSerialized)) {
                sideSerialized = sideSerialized.join(',');
            }
            localVarQueryParameters['side'] = sideSerialized;
        }

        if (opts.status !== undefined) {
            let statusSerialized = ObjectSerializer.serialize(opts.status, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(statusSerialized)) {
                statusSerialized = statusSerialized.join(',');
            }
            localVarQueryParameters['status'] = statusSerialized;
        }

        if (opts.from !== undefined) {
            let fromSerialized = ObjectSerializer.serialize(opts.from, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(fromSerialized)) {
                fromSerialized = fromSerialized.join(',');
            }
            localVarQueryParameters['from'] = fromSerialized;
        }

        if (opts.to !== undefined) {
            let toSerialized = ObjectSerializer.serialize(opts.to, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(toSerialized)) {
                toSerialized = toSerialized.join(',');
            }
            localVarQueryParameters['to'] = toSerialized;
        }

        if (opts.limit !== undefined) {
            let limitSerialized = ObjectSerializer.serialize(opts.limit, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitSerialized)) {
                limitSerialized = limitSerialized.join(',');
            }
            localVarQueryParameters['limit'] = limitSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = ['apiv4'];
        return this.client.request<Array<OrderResponse>>(config, 'Array<OrderResponse>', authSettings);
    }

    /**
     * Order placement interface, rate-limited at 5 requests per second per user.
     * @summary Alpha Order API
     * @param placeOrderRequest
     */
    public async placeAlphaOrder(
        placeOrderRequest: PlaceOrderRequest,
    ): Promise<{ response: AxiosResponse; body: PlaceOrderResponse }> {
        const localVarPath = this.client.basePath + '/alpha/orders';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        // verify required parameter 'placeOrderRequest' is not null or undefined
        if (placeOrderRequest === null || placeOrderRequest === undefined) {
            throw new Error('Required parameter placeOrderRequest was null or undefined when calling placeAlphaOrder.');
        }

        const config: AxiosRequestConfig = {
            method: 'POST',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
            data: ObjectSerializer.serialize(placeOrderRequest, 'PlaceOrderRequest'),
        };

        const authSettings = ['apiv4'];
        return this.client.request<PlaceOrderResponse>(config, 'PlaceOrderResponse', authSettings);
    }

    /**
     *
     * @summary Alpha Single Order Query API
     * @param orderId Order ID
     */
    public async getAlphaOrder(orderId: string): Promise<{ response: AxiosResponse; body: OrderResponse }> {
        const localVarPath = this.client.basePath + '/alpha/order';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        // verify required parameter 'orderId' is not null or undefined
        if (orderId === null || orderId === undefined) {
            throw new Error('Required parameter orderId was null or undefined when calling getAlphaOrder.');
        }

        let orderIdSerialized = ObjectSerializer.serialize(orderId, 'string');
        // For array query parameters with style:form and explode:false, convert to comma-separated string
        if (Array.isArray(orderIdSerialized)) {
            orderIdSerialized = orderIdSerialized.join(',');
        }
        localVarQueryParameters['order_id'] = orderIdSerialized;

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = ['apiv4'];
        return this.client.request<OrderResponse>(config, 'OrderResponse', authSettings);
    }

    /**
     * When currency is provided, query and return specified currency information; when currency is not provided, return paginated currency list
     * @summary Query currency information
     * @param opts Optional parameters
     * @param opts.currency Query currency information by currency symbol
     * @param opts.limit Maximum number of records returned in a single list
     * @param opts.page Page number
     */
    public async listAlphaCurrencies(opts: {
        currency?: string;
        limit?: number;
        page?: number;
    }): Promise<{ response: AxiosResponse; body: Array<Currency2> }> {
        const localVarPath = this.client.basePath + '/alpha/currencies';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.currency !== undefined) {
            let currencySerialized = ObjectSerializer.serialize(opts.currency, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(currencySerialized)) {
                currencySerialized = currencySerialized.join(',');
            }
            localVarQueryParameters['currency'] = currencySerialized;
        }

        if (opts.limit !== undefined) {
            let limitSerialized = ObjectSerializer.serialize(opts.limit, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitSerialized)) {
                limitSerialized = limitSerialized.join(',');
            }
            localVarQueryParameters['limit'] = limitSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = [];
        return this.client.request<Array<Currency2>>(config, 'Array<Currency2>', authSettings);
    }

    /**
     * When currency is provided, returns ticker information for the specified currency. When currency is not provided, returns paginated ticker list
     * @summary Query currency ticker
     * @param opts Optional parameters
     * @param opts.currency Query by specified currency name
     * @param opts.limit Maximum number of records returned in a single list
     * @param opts.page Page number
     */
    public async listAlphaTickers(opts: {
        currency?: string;
        limit?: number;
        page?: number;
    }): Promise<{ response: AxiosResponse; body: Array<AlphaTicker> }> {
        const localVarPath = this.client.basePath + '/alpha/tickers';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.currency !== undefined) {
            let currencySerialized = ObjectSerializer.serialize(opts.currency, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(currencySerialized)) {
                currencySerialized = currencySerialized.join(',');
            }
            localVarQueryParameters['currency'] = currencySerialized;
        }

        if (opts.limit !== undefined) {
            let limitSerialized = ObjectSerializer.serialize(opts.limit, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(limitSerialized)) {
                limitSerialized = limitSerialized.join(',');
            }
            localVarQueryParameters['limit'] = limitSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = [];
        return this.client.request<Array<AlphaTicker>>(config, 'Array<AlphaTicker>', authSettings);
    }

    /**
     * Supports passing chain, platform, and address
     * @summary Query Token Information
     * @param opts Optional parameters
     * @param opts.chain Query Token List by Chain  - solana - eth - bsc - base - world - sui - arbitrum - avalanche - polygon - linea - optimism - zksync - gatelayer
     * @param opts.launchPlatform Query Token List by Launch Platform  - meteora_dbc - fourmeme - moonshot - pump - raydium_launchlab - letsbonk - gatefun - virtuals
     * @param opts.address Query Token List by Contract Address
     * @param opts.page Page number
     */
    public async listAlphaTokens(opts: {
        chain?: string;
        launchPlatform?: string;
        address?: string;
        page?: number;
    }): Promise<{ response: AxiosResponse; body: Array<Tokens> }> {
        const localVarPath = this.client.basePath + '/alpha/tokens';
        let localVarQueryParameters: any = {};
        let localVarHeaderParams: any = (<any>Object).assign({}, this.client.defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        if (produces.indexOf('application/json') >= 0) {
            localVarHeaderParams.Accept = 'application/json';
        } else {
            localVarHeaderParams.Accept = produces.join(',');
        }

        opts = opts || {};
        if (opts.chain !== undefined) {
            let chainSerialized = ObjectSerializer.serialize(opts.chain, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(chainSerialized)) {
                chainSerialized = chainSerialized.join(',');
            }
            localVarQueryParameters['chain'] = chainSerialized;
        }

        if (opts.launchPlatform !== undefined) {
            let launchPlatformSerialized = ObjectSerializer.serialize(opts.launchPlatform, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(launchPlatformSerialized)) {
                launchPlatformSerialized = launchPlatformSerialized.join(',');
            }
            localVarQueryParameters['launch_platform'] = launchPlatformSerialized;
        }

        if (opts.address !== undefined) {
            let addressSerialized = ObjectSerializer.serialize(opts.address, 'string');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(addressSerialized)) {
                addressSerialized = addressSerialized.join(',');
            }
            localVarQueryParameters['address'] = addressSerialized;
        }

        if (opts.page !== undefined) {
            let pageSerialized = ObjectSerializer.serialize(opts.page, 'number');
            // For array query parameters with style:form and explode:false, convert to comma-separated string
            if (Array.isArray(pageSerialized)) {
                pageSerialized = pageSerialized.join(',');
            }
            localVarQueryParameters['page'] = pageSerialized;
        }

        const config: AxiosRequestConfig = {
            method: 'GET',
            params: localVarQueryParameters,
            headers: localVarHeaderParams,
            url: localVarPath,
        };

        const authSettings = [];
        return this.client.request<Array<Tokens>>(config, 'Array<Tokens>', authSettings);
    }
}
